#include "Menu.h"
using namespace std;

int main()
{
    cout << "Welcome to the PIG game!" << endl << endl;
    Menu menu;
    
    if(menu.getGameMode() == 'A')
      Core game;
    else
      Double game;
}